#!/usr/bin/env python3
# This script is a stub. Functionality has moved to basebuddy.runner.
import sys
print("Warning: bb_runners.py is deprecated. Its functionality is now in 'basebuddy.runner'.", file=sys.stderr)
# Optional: Add stubs for specific old functions if vital for transition,
# otherwise, a general warning suffices.
# Example:
# def simulate_short_reads_runner(*args, **kwargs):
#     print("Error: simulate_short_reads_runner is deprecated. Use functions from basebuddy.runner.", file=sys.stderr)
#     sys.exit(1)
if __name__ == "__main__":
    print("bb_runners.py was a library and not meant to be run directly.", file=sys.stderr)
    sys.exit(1)
